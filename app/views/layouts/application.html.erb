<!DOCTYPE html>
<html>
  <head>
    <title>FeZeroWasteKitchen</title>
    <link rel="icon" type="image/x-icon" href="assets/image.png">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap -->
      <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap/dist/css/bootstrap.min.css' %>
      <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/bootstrap/dist/js/bootstrap.bundle.min.js' %>

      <!-- MDB UI KIT -->
      <%= stylesheet_link_tag 'https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/3.3.0/mdb.min.css' %>
      <%= javascript_include_tag 'https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.js' %>

      <%# tagify %>
          <%= stylesheet_link_tag 'https://code.jquery.com/jquery-3.6.0.min.js' %>
          <%= javascript_include_tag 'https://code.jquery.com/jquery-3.6.0.min.js' %>

      <%# select2 %>
    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/select2/dist/css/select2.min.css' %>
    <%= javascript_include_tag 'https://cdn.jsdelivr.net/npm/select2/dist/js/select2.full.min.js' %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Flash messages -->
    <% flash.each do |key, message| %>
      <div class="alert alert-<%= key %>">
        <%= message %>
      </div>
    <% end %>

    <nav class="navbar navbar-expand-sm">
        <a class="nav-item nav-link navbar-brand text-dark" href="<%= root_path %>">Home</a>

      <% if current_user %>
        <a class="nav-item nav-link navbar-brand text-dark" href="<%= user_path(current_user.id) %>">Kitchen</a>
      <% else %>
        <a class="nav-item nav-link navbar-brand text-dark" href="<%= request.path %>"onclick="showSignInMessage()">Kitchen</a>
      <% end %>

      <a class="nav-item nav-link navbar-brand text-dark" href="<%= search_index_path %>">Search</a>
    </nav>

    <script>
      function showSignInMessage() {alert("Please sign in to access the Kitchen!");}
    </script>
    <%= yield %>
  </body>
</html>
